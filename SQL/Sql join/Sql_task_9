Представим, что в логирование DeviceID в событиях закралась ошибка - часть ID была записана в базу некорректно. Это привело к тому, что в таблице с событиями появились DeviceID, для которых нет инсталлов. 
Нам надо отобрать примеры DeviceID из таблицы event, которых нет в таблице installs, чтобы отправить их команде разработчиков на исправление. 
Выведите 10 уникальных DeviceID, которые присутствуют в таблице events, но отсутствуют в installs, отсортировав их в порядке убывания.

Для того, чтобы решить это задание, можно использовать присутствующий в Clickhouse LEFT ANTI JOIN - черный список по ключам соединения, не производит декартово произведение.


SELECT 
    DISTINCT(DeviceID)
FROM 
    events AS ev 
LEFT ANTI JOIN
    installs AS i
ON 
    i.DeviceID=ev.DeviceID
ORDER BY 
    DeviceID DESC
LIMIT 10
